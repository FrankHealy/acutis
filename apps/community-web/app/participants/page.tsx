import { productText } from "@acutis/localization";
export default function ParticipantsPage() { return <main><h1>{productText("en-IE", "common.participants")}</h1><p>{productText("en-IE", "community.participants")}</p></main>; }
