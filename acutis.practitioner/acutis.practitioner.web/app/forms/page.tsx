import { productText } from "@acutis/localization";
export default function FormsPage() { return <main><h1>{productText("en-IE", "common.forms")}</h1><p>{productText("en-IE", "practitioner.forms")}</p></main>; }
