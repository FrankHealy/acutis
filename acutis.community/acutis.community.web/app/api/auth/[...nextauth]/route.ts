import NextAuth from"next-auth";import{createProductAuthOptions}from"@acutis/identity/server";const handler=NextAuth(createProductAuthOptions({clientId:process.env.KEYCLOAK_CLIENT_ID??"acutis-community-web",clientSecret:process.env.KEYCLOAK_CLIENT_SECRET??"local-public-client",issuer:process.env.KEYCLOAK_ISSUER??"http://localhost:8080/realms/acutis-community"}));export{handler as GET,handler as POST};
