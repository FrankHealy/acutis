"use client";import type{ReactNode}from"react";import{ProductMembershipGate}from"@acutis/identity/react";export default function Membership({children}:{children:ReactNode}){return <ProductMembershipGate apiBaseUrl={process.env.NEXT_PUBLIC_API_URL??"http://localhost:5020"} unavailable={<main><h1>Access unavailable</h1><p>A Community tenant membership and role are required.</p></main>}>{children}</ProductMembershipGate>}
