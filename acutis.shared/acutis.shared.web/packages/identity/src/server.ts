import type{NextAuthOptions}from"next-auth";import KeycloakProvider from"next-auth/providers/keycloak";
export function createProductAuthOptions(config:{clientId:string;clientSecret:string;issuer:string;identityProviderHint?:string}):NextAuthOptions{return{providers:[KeycloakProvider({clientId:config.clientId,clientSecret:config.clientSecret,issuer:config.issuer,...(config.identityProviderHint?{authorization:{params:{kc_idp_hint:config.identityProviderHint}}}:{})})],callbacks:{async jwt({token,account}){if(account?.access_token)token.accessToken=account.access_token;return token},async session({session,token}){(session as typeof session&{accessToken?:string}).accessToken=token.accessToken as string|undefined;return session}}};}
